# Система поддержки принятия решений (СППР)

Система для оптимизации планирования ресурсов, построенная на FastAPI и SQLite.

## Описание

Система не принимает решения за пользователя, а предлагает несколько альтернатив распределения ресурсов по задачам. Для каждой альтернативы выводится пояснение, почему она предложена.

## Возможности

- **🎨 Красивый веб-интерфейс** - удобный графический интерфейс вместо сложного Swagger
- **Полное CRUD управление** ресурсами и задачами (создание, чтение, обновление, удаление)
- **Выпадающие списки** для выбора типов ресурсов и приоритетов задач
- Генерация альтернатив распределения ресурсов с пояснениями
- Несколько алгоритмов распределения:
  - По приоритету задач
  - Равномерное распределение
  - С учетом специализации ресурсов
  - С минимизацией перегрузки
- **Статистика и аналитика** распределения с предупреждениями о перегрузке
- **Визуализация данных** - карточки, таблицы, графики загрузки
- **Экспорт данных** в JSON и CSV форматах
- Автоматическая валидация данных и проверка на перегрузку ресурсов

## Установка

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

## Запуск

Запустите приложение с помощью uvicorn:

```bash
uvicorn main:app --reload
```

Приложение будет доступно по адресу: http://localhost:8000

**Веб-интерфейс:** http://localhost:8000 (красивый графический интерфейс)

**API документация:** http://localhost:8000/docs (Swagger UI)

## API Endpoints

### Ресурсы

- `POST /resource` - Добавить ресурс
- `GET /resources` - Получить все ресурсы
- `GET /resource/{id}` - Получить ресурс по ID
- `PUT /resource/{id}` - Обновить ресурс
- `DELETE /resource/{id}` - Удалить ресурс

### Задачи

- `POST /task` - Добавить задачу
- `GET /tasks` - Получить все задачи
- `GET /task/{id}` - Получить задачу по ID
- `PUT /task/{id}` - Обновить задачу
- `DELETE /task/{id}` - Удалить задачу

### Альтернативы

- `GET /alternatives` - Получить альтернативы распределения ресурсов
- `GET /alternative/{id}` - Получить альтернативу по ID

### Статистика

- `GET /stats?alternative_id={id}` - Получить статистику распределения (с предупреждениями о перегрузке)

### Экспорт

- `GET /export/alternatives?format=json` - Экспорт альтернатив в JSON
- `GET /export/alternatives?format=csv` - Экспорт альтернатив в CSV

### Вспомогательные

- `POST /load-example-data` - Загрузить примерные данные для тестирования
- `GET /docs` - Интерактивная документация API (Swagger UI)

## Примеры использования

### 1. Загрузка примерных данных

```bash
curl -X POST "http://localhost:8000/load-example-data"
```

### 2. Добавление ресурса

```bash
curl -X POST "http://localhost:8000/resource" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Иван Иванов",
    "type": "разработчик",
    "available_hours": 160.0
  }'
```

### 3. Добавление задачи

```bash
curl -X POST "http://localhost:8000/task" \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Разработка нового функционала",
    "required_hours": 200.0,
    "priority": 1
  }'
```

### 4. Получение альтернатив

```bash
curl -X GET "http://localhost:8000/alternatives"
```

## Структура проекта

```
.
├── main.py              # Главный файл FastAPI приложения
├── models.py            # Модели SQLAlchemy (Resource, Task, Alternative, Allocation)
├── database.py          # Настройка подключения к БД
├── schemas.py           # Pydantic схемы для валидации данных
├── crud.py              # CRUD операции для работы с БД
├── algorithms.py        # Алгоритмы генерации альтернатив
├── example_data.py      # Примерные данные для тестирования
├── requirements.txt     # Зависимости проекта
├── README.md           # Документация
└── sppr.db             # База данных SQLite (создается автоматически)
```

## База данных

Система использует SQLite базу данных `sppr.db`, которая создается автоматически при первом запуске.

### Модели данных

- **Resource**: Ресурсы (люди, оборудование и т.д.)
  - id, name, type, available_hours

- **Task**: Задачи/проекты
  - id, title, required_hours, priority (1-5)

- **Alternative**: Альтернативы распределения
  - id, explanation, score

- **Allocation**: Распределение ресурса на задачу
  - id, alternative_id, resource_id, task_id, hours

## Алгоритмы распределения

Система генерирует 4 типа альтернатив:

1. **По приоритету задач** - высокоприоритетные задачи получают ресурсы первыми
2. **Равномерное распределение** - пропорциональное распределение ресурсов
3. **С учетом специализации** - использование различных типов ресурсов
4. **Минимизация перегрузки** - равномерная загрузка всех ресурсов

Каждая альтернатива получает оценочный балл и текстовое пояснение.

## Расширение системы

Код содержит подробные комментарии для облегчения расширения:

- Добавление новых алгоритмов в `algorithms.py`
- Добавление новых полей в модели в `models.py`
- Добавление новых endpoints в `main.py`
- Модификация схем валидации в `schemas.py`

## Лицензия

Проект создан для образовательных целей.

